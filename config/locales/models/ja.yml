ja:
  activerecord:
    models:
      task:
    attributes:
      task:
        title: Todo名
        detail: 詳細
        deadline: 締切り
    errors:
      models:
        task:
          attributes:
            title:
              format: '%{message}'
              blank: '%{attribute} を入力してください。'
            detail:
              format: '%{message}'
              blank: '%{attribute} を入力してください。'
            deadline:
              format: '%{message}'
              blank: '%{attribute} を入力してください。'
  activerecord:
    models:
      user:
    attributes:
      user:
        name: 名前
        password: パスワード        
    errors:
      models:
        user:
          attributes:
            name:
              format: '%{message}'
              blank: '%{attribute} を入力してください。'
            password:
              format: '%{attribute} %{message}'
              blank: 'を入力してください。'